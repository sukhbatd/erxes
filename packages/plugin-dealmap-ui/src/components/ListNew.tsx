import React, { type FC } from 'react';
import { IDealmap } from '../types';

interface ListNewProps {
  dealmaps: IDealmap[];
}

const ListNew: FC<ListNewProps> = ({ dealmaps }) => {
  return <div>hi</div>;
};
export default ListNew;
